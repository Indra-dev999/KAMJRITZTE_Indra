pyspark==3.4.0       # PySpark for processing data
pytest==7.4.0        # Testing framework
Flask==2.3.3         # Flask for API (if used)
pandas==2.1.3        # For additional data manipulation, if needed
sqlalchemy==2.0.21   # If interacting with databases
requests==2.31.0     # For making HTTP requests
apache-airflow==2.7.3 # Airflow for scheduling
